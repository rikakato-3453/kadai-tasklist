<% if users.any? %>
  <% users.each do |user| %>
    <table>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to 'View profile', user_path(user) %></td>
      </tr>
    </table>
  <% end  %> 
<% end %>